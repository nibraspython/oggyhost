{
  "name": "vercel-api",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "dev": "npx vercel dev",
    "start": "npx vercel start",
    "deploy": "npx vercel deploy"
  },
  "dependencies": {
    "node-fetch": "^3.3.0"
  },
  "devDependencies": {
    "vercel": "latest"
  }
}
